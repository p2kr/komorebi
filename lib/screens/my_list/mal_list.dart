import 'package:flutter/material.dart';

class MalList extends StatelessWidget {
  const MalList({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
