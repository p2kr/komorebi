import 'package:flutter/material.dart';
import 'package:komorebi/src/core/utils/utils.dart';

class SelectedSandboxScreen extends StatelessWidget {
  const SelectedSandboxScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Placeholder(color: getRandomColor());
  }
}
